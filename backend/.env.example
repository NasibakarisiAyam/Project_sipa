# Example environment variables for database connection
# Copy this file to `.env` or set these variables in your hosting control panel.

DB_HOST=db
DB_USER=root
DB_PASS=
DB_NAME=bookflix

# Optional: change these if your PHP install needs them
# PHP_DISPLAY_ERRORS=0