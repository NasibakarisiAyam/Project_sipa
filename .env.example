### Copy this file to `.env` and fill secure values before running `docker compose up`.
# Used by docker-compose variable substitution. Do NOT commit sensitive values.

# MySQL credentials for the container
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=bookflix
MYSQL_USER=bookflix
MYSQL_PASSWORD=secret_password

# Apache port mapping on host
APACHE_PORT=8080
